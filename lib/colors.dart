import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const Color lightPrimary = Color(0xFF9AD0C2);
  static const Color lightBackground = Color(0xFFEBF8EC);
  static const Color lightAccent = Color(0xFF52859E);

  // Dark theme colors
  static const Color darkPrimary = Color(0xFF244F76);
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkAccent = Color(0xFF52859E);
}
